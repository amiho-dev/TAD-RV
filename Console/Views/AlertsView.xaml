<!-- AlertsView.xaml — Alert and event log viewer -->
<UserControl x:Class="TadConsole.Views.AlertsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource PrimaryBrush}">
    <DockPanel Margin="32,28">
        <StackPanel DockPanel.Dock="Top" MaxWidth="900" HorizontalAlignment="Left">
            <TextBlock Text="Alerts &amp; Logs" Style="{StaticResource SectionHeader}" />
        </StackPanel>

        <!-- Toolbar -->
        <Border DockPanel.Dock="Top" Style="{StaticResource Card}" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal">
                <TextBox Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"
                         Width="280" Padding="8,6"
                         FontSize="13"
                         Background="#0D1117"
                         Foreground="{StaticResource TextPrimaryBrush}"
                         BorderBrush="{StaticResource BorderBrush}" />
                <Button Content="Refresh"
                        Style="{StaticResource AccentButton}"
                        Command="{Binding RefreshCommand}"
                        Margin="12,0,0,0" />
                <Button Content="Clear"
                        Style="{StaticResource AccentButton}"
                        Command="{Binding ClearCommand}"
                        Background="{StaticResource DangerBrush}"
                        Margin="8,0,0,0" />
            </StackPanel>
        </Border>

        <!-- Alert List -->
        <ListBox ItemsSource="{Binding Alerts}"
                 Background="Transparent" BorderThickness="0"
                 Foreground="{StaticResource TextSecondaryBrush}"
                 FontFamily="Consolas" FontSize="12" />
    </DockPanel>
</UserControl>
